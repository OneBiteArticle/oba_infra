services:
  ai_nginx:
    image: nginx:1.25
    container_name: oba-nginx
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "80:80"
    networks:
      - oba_shared_network
    restart: always

networks:
  oba_shared_network:
    external: true
